# Keymetrics-Widget

Edit token and public_key in main.js
```javascript
// Keymetrics config
var km = new Keymetrics({
  token: '',
  public_key: '',
  realtime: true
});
```

Install and start
```
npm i && npm start
```